import 'package:flutter/material.dart';

// Define light theme
final ThemeData lightTheme = ThemeData(
    primaryColor: Colors.indigo, fontFamily: "sf-pro-Text" // Your primary color
    // Add other theme properties (e.g., accentColor, textTheme, etc.)
    );
